package com.example.demo.repositories;

import com.example.demo.entities.FeedItemScore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FeedItemScoreRepository extends JpaRepository<FeedItemScore, Long>
{
}
